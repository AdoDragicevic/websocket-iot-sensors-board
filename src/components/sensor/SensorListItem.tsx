import { SensorListItemProps } from "../../models/props";

const SensorListItem = ({ sensor, onToggleSensorConnection }: SensorListItemProps) => {
  return (
    <li>
      <article>
        <p>hi</p>
      </article>
    </li>
  )
};

export default SensorListItem;